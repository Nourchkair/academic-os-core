[project]
name = "academic-os-core"
version = "0.1.0"
description = "Reusable local academic operating system templates and installer"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
